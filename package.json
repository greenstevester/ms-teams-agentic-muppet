{
  "name": "ms-teams-agentic-muppet",
  "version": "0.1.0",
  "description": "Public-only agent for Microsoft Teams, modelled on Shopify's River",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "botbuilder": "^4.23.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/express": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
